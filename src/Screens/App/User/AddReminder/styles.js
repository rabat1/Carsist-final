import { StyleSheet } from "react-native"
import Colors from "../../../../Utils/Colors";

export default StyleSheet.create({
  base: {
    backgroundColor: "#DEEFCB",
    height: '100%'
  },
  text: {
    color: Colors.primary,
    fontSize: 20,
    fontFamily: 'monospace'
  }

});